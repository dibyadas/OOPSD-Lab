import java.io.*;

class one
{
	public static void main(String args[]) throws Exception {
		DataInputStream stream = new DataInputStream(System.in);
		String k1,k2,p;
		k1 = stream.readLine();
		k2 = stream.readLine();
		System.out.println(k1.substring(1)+k2.substring(1));
	}
}
