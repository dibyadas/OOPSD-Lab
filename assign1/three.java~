import java.io.*;

class three
{
	public static void main(String args[]) throws Exception {
		DataInputStream stream = new DataInputStream(System.in);
		String k1,k2,p;
		k1 = stream.readLine();
		int m = Integer.parseInt(k1);
		//k2 = stream.readLine();
		//System.out.println(k1.substring(0,2)+k1.substring(4));
		k2 = Integer.toString(m*2);
		System.out.println(k2.substring(0,1)+k2.substring(3));
	}
}
